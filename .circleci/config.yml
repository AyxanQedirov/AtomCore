version: 2.1

jobs:
  job-1:
    docker:
      - image: mcr.microsoft.com/dotnet/sdk:8.0
    steps:
      - checkout
      - run:
          name: Check Git pull
          command: ls
  job-2:
    docker:
      - image: cimg/base:2022.06
    steps:
      - run:
          name: PETESH baby come
          command: whoami

workflows:
  version: 2
  pipeline-1:
    jobs:
      - job-1
      - job-2:
          requires:
            - job-1

      