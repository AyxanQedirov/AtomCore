version: 2.1

jobs:
  print-hello-world:
    docker:
      - image: circleci/node:14
    steps:
      - checkout
      - run:
          name: Print hello world
          command: |
              echo "Son commit mesajı:"
              git log -1 --pretty=%B

workflows:
  version: 2
  hello_world_flow:
    jobs:
      - print-hello-world:
          filters:
            branches:
              only:
                - master